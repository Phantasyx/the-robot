# Local / Ollama provider (default for offline use)
ROBOT_PROVIDER=ollama
ROBOT_OLLAMA_HOST=http://127.0.0.1:11434
ROBOT_MODEL=llama3.2

# Approval gates: prompt | auto-approve | deny
ROBOT_APPROVAL_MODE=prompt

# Sandbox root for built-in file tools (list_dir / read_file / write_file)
# ROBOT_WORKSPACE=/path/to/workspace

# Optional destructive shell tool (off by default)
# ROBOT_ENABLE_RUN_COMMAND=1

# Skills and routines roots (optional overrides)
# ROBOT_SKILLS_DIR=./skills
# ROBOT_ROUTINES_DIR=./routines

# Persistent data (conversation JSON store under <dir>/conversations)
# ROBOT_DATA_DIR=~/.the-robot

# Max model↔tool rounds in a live session
# ROBOT_MAX_TOOL_ROUNDS=4

# run_command hard timeout (ms); tool is argv-only / no shell
# ROBOT_RUN_COMMAND_TIMEOUT_MS=15000
